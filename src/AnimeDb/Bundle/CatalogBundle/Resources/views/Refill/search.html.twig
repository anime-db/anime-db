<div class="b-refill-search">
	{% if result is not empty %}
		<span>{% trans %}Plugin found some items, will choose the necessary. Link to identify the record can be added to the item sources automatically, in which case the search will not proceed further.{% endtrans %}</span>
		<ul>
			{% for item in result %}
				<li><a href="{{ item.link }}" title="{% trans %}Use to fill{% endtrans %}">{{ item.name }}</a></li>
			{% endfor %}
		</ul>
	{% else %}
		<span>{% trans %}Nothing found{% endtrans %}</span>
	{% endif %}
</div>