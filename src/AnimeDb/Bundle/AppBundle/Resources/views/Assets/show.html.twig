{% for path in js %}
	<script type="text/javascript" src="{{ asset(path) }}"></script>
{% endfor %}
{% for path in css %}
	<link href="{{ asset(path) }}" type="text/css" rel="stylesheet">
{% endfor %}